{{- if .Values.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ template "namespace.gatewayName" . }}
  namespace: {{ template "namespace.gatewayNamespace" . }}
  labels:
    {{- include "namespace.labels" . | nindent 4 }}
spec:
  gatewayClassName: {{ .Values.gateway.className }}
  listeners:
    {{- if .Values.gateway.httpListener.enabled }}
    - allowedRoutes:
        namespaces:
          from: Selector
          selector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.magicnamespace.namespace }}
      name: http
      port: 80
      protocol: HTTP
    {{- end }}
    {{- if .Values.gateway.httpsListener.enabled }}
    - allowedRoutes:
        namespaces:
          from: Selector
          selector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.magicnamespace.namespace }}
      name: https
      port: 443
      protocol: HTTPS
      tls:
        mode: Terminate
        certificateRefs:
          {{- with .Values.gateway.httpsListener.certificateRefs }}
            {{- toYaml . | nindent 10 }}
          {{- end }}
    {{- end }}
    {{- with .Values.gateway.extraListeners }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  infrastructure:
    {{- if .Values.gateway.envoyProxyConfig.enabled }}
    parametersRef:
      group: gateway.envoyproxy.io
      kind: EnvoyProxy
      name: {{ template "namespace.gatewayName" . }}
    {{- else }}
    {{- with .Values.gateway.parametersRef }}
    parametersRef:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- end }}
{{- end }}
