apiVersion: v1
kind: Service
metadata:
  name: {{ include "alma-kickstart.fullname" . }}
  labels:
    {{- include "alma-kickstart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alma-kickstart.selectorLabels" . | nindent 4 }}
